<% provide(:title, '模擬店一覧') %>
<h1>模擬店ユーザー一覧</h1>

<ul data-role="listview" data-inset="true">
  <% @users.each do |user| %>
    <li>
      (<%= user.id %>)<%= user.name %><br>
      <%= user.email %>
    </li>
  <% end %>
</ul>