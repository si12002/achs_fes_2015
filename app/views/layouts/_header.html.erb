<div data-role="header">
	<% if signed_in? and current_user.role_id != 3 %>
		<%= link_to "マイページ", current_user %>
	<% else %>
		<%= link_to "ホーム", root_path %>
	<% end %>
<h1><%= full_title(yield(:title)) %></h1>

<%= link_to "メニュー", "#navpanel" %>
</div>